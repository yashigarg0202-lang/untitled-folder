services:
  - type: web
    name: soulsync-manifestation
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt || true"
    startCommand: "python3 server.py"
    envVars:
      - key: ADMIN_USER
        value: pooja
      - key: ADMIN_PASS
        value: soulsync2026!
